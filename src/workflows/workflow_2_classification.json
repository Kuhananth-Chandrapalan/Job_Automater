[
    {
        "name": "Cron (Priority)",
        "type": "n8n-nodes-base.cron",
        "parameters": {
            "mode": "everyX",
            "value": 15,
            "unit": "minutes"
        }
    },
    {
        "name": "Read Unclassified",
        "type": "n8n-nodes-base.googleSheets",
        "parameters": {
            "operation": "read",
            "sheetId": "YOUR_SHEET_ID",
            "range": "A:T",
            "filters": {
                "status": [
                    "New"
                ]
            }
        }
    },
    {
        "name": "Classifier Function",
        "type": "n8n-nodes-base.function",
        "parameters": {
            "functionCode": "/* PASTE CONTENT FROM src/utils/classifier.py here (translated to JS) */\n\nconst items = $items('Read Unclassified');\nreturn items.map(item => {\n  const title = item.json.role_title;\n  const domains = {'Data Science': ['data', 'ml'], 'Other': []};\n  // ... implement logic ...\n  return { json: { job_id: item.json.job_id, priority_score: 5, domain: 'Data Science' } };\n});"
        }
    },
    {
        "name": "Update Sheet",
        "type": "n8n-nodes-base.googleSheets",
        "parameters": {
            "operation": "update",
            "sheetId": "YOUR_SHEET_ID",
            "columnToMatchOn": "job_id"
        }
    }
]